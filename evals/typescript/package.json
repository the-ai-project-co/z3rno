{
  "name": "@z3rno/evals",
  "version": "0.23.0",
  "private": true,
  "description": "TypeScript eval harness for z3rno, scored against the shared golden_v1 fixture.",
  "license": "Apache-2.0",
  "type": "commonjs",
  "engines": {
    "node": ">= 18"
  },
  "scripts": {
    "build": "tsc",
    "test": "npm run build && node --test dist/test/*.test.js"
  },
  "dependencies": {
    "@z3rno/sdk": "file:../../bindings/typescript"
  },
  "devDependencies": {
    "typescript": "^5",
    "@types/node": "^20"
  }
}
