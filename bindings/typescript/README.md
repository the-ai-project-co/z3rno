# bindings/typescript

TypeScript binding scaffold for z3rno, built with napi-rs. No real API yet —
this only proves the native-addon build toolchain works end-to-end, exposing
a single `hello()` smoke-test function.

The real TypeScript bindings land in slice 0006.
