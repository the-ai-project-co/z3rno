[workspace]

[package]
name = "surrealdb-spike"
version = "0.0.0"
edition = "2021"
publish = false

[dependencies]
tokio = { version = "1", features = ["macros", "rt-multi-thread"] }
rand = "0.8"
surrealdb = { version = "3.2.4", features = ["protocol-ws"] }
