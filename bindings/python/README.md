# bindings/python

Python binding scaffold for z3rno, built with PyO3 and maturin. No real API
yet — this only proves the build toolchain works end-to-end, exposing a
single `hello()` smoke-test function.

The real Python bindings land in slice 0006.
